<p style="color: green"><%= notice %></p>

<% content_for :title, "Temperatures" %>

<h1>Temperatures</h1>

<div id="temperatures">
  <% @temperatures.each do |temperature| %>
    <p>
      <%= temperature.value %>°C <%= time_ago_in_words(temperature.created_at) %> ago <%= link_to "e", temperature %>
    </p>

  <% end %>
</div>

<%= link_to "New temperature", new_temperature_path %>
